--因为我们是基于openvla这个代码库去构建代码的，所以我们实现的craft相关功能的代码要整理好，分好文件夹，否则和openvla的自带文件混在一起，非常乱。

--代码一定要分好文件夹，做好归类，所有说明文档也要分好文件夹，不同类的说明文档放在对应文件夹里

--代码实现以后，尽量写好训练或者推理的各种相关功能的sh文件接口，方便用户直接调用

--我们是在远程服务器上，8张v100跑代码，远程服务器用不了code agent，因此我们是在本地用code agent修改代码，然后git上传，再在服务器端pull，做到代码修改同步，因为我们是本地修改代码，本地不运行，所以你把代码写好即可，不要运行或者测试。不要尝试运行任何代码文件，把代码写好了就可以了 


--之后我们做实验的流程是我把终端报错发给你，你给出回答解决问题 

--注意我们的微调数据选择libero数据集

--用户发给你的指令里可能会有一些你已经做过了的内容，如果你做过了，那就可自己决定跳过还是再检查一遍顺便优化优化，如果发给你的指令里有你没做过的，那就按照他的意思去做（注意修改代码过程中，不一定严格按照用户逻辑来，因为你可以直接接触代码，如果发现用户的一些要求是错的，或执行用户的要求以后会产生和代码库冲突的bug或者错误，那么你就自己做决定怎么修改，以你为主）

--如果用户的需求出现和IDEA.md内容冲突的，你自己决定，你是第一手接触代码的角色，给你充分的自由决策权

--不需要死板地严格按照用户要求，你要根据代码库的实际情况来做决策

--如果commit的时候有submodule，比如LIBERO，就忽略它

--你不需要做任何测试，把代码写好即可。即使prompt里有让你做测试，你也不用去做（因为我们环境还没配好，等配好了我们自行去做）

--写代码的时候有一个shuffle buffer size要设置，否则TensorFlow加载数据集会把内存爆满把镜像搞崩（可能是训练的时候，具体我忘记了）

--修改代码的时候允许在文件上直接修改而不新建文件，如果新建了一个功能重复的文件，需要把旧版的废弃文件删除，每次做了代码修改，要把说明文档应该修改的内容也都修改了